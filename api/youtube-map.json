---
permalink: /api/youtube-map.json
---
{
  {% assign all_docs = site.pages | concat: site.documents %}
  {% assign video_pages = all_docs | where_exp: "item", "item.youtube_id != nil" %}
  {% for item in video_pages %}
    "{{ item.youtube_id }}": "{{ item.url }}"{% unless forloop.last %},{% endunless %}
  {% endfor %}
}